{
  "scripts": {
    "dev": "tsx --env-file=.env --use-system-ca main",
    "build": "esbuild main.ts --bundle --platform=node --outfile=dist/main.js --format=esm --external:node:*",
    "start": "node --use-system-ca dist/main"
  },
  "type": "module",
  "packageManager": "pnpm@10.28.2",
  "dependencies": {
    "node-html-parser": "^7.0.2"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "esbuild": "^0.27.2"
  }
}